#include <stdio.h>
#include <math.h>
typedef struct diem {
int x ,y;
};
int dodaitoado(int ){
	
}
int main (){
	int n;
    scanf ("%d",&n);
	diem a[n];
	for (int i=1;i<=n;i++){
	scanf("%d %d ",&a[i].x,&a[i].y);
}
    for (int i=1;i<=n;i++){
    	printf("%d %d ",a[i].x,a[i].y);
	}
	
}
